- The method `spin_me` uses `String#split` to split the string into its individual words.
- The words are returned in a new array which doesn't reference the original string.
- Therefore, the returned string will be a different object from the one passed to the method.
